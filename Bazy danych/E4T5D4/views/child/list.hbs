<h2>Lista dzieci</h2>
<ul>
    {{#each childrenList}}
        <li>
            <form action="/child/gift/{{this.id}}?_method=PATCH" method="POST">
                <label>
                    {{this.name}}
                    <select name="giftId">
                        <option value="">Brak</option>
                        {{#each ../giftsList}}
                            <option value="{{this.id}}" {{#if (equals this.id ../giftId)}}selected="selected"{{/if}}>{{this.name}}</option>
                        {{/each}}
                    </select>
                </label>
                <button class="btn btn-light">Zapisz</button>
            </form>
        </li>
    {{/each}}
</ul>

<h2>Dodaj dziecko do listy</h2>
<form action="/child" method="POST">
    <label for="">
        Imię
        <input name="name" type="text">
    </label>
    <button class="btn btn-light ">Dodaj</button>
</form>